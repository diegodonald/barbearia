import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  // Extensões padrão do Next.js e TypeScript
  ...compat.extends("next/core-web-vitals", "next/typescript"),

  // Configurações adicionais
  {
    files: ["**/*.{ts,tsx}"],
    rules: {
      "@typescript-eslint/no-explicit-any": "warn", // Emite aviso ao invés de erro para uso de 'any'
      "@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_" }], // Ignora argumentos com prefixo '_'
      "@typescript-eslint/explicit-module-boundary-types": "off", // Desativa obrigatoriedade de tipos de retorno explícitos
    },
  },
];

export default eslintConfig;
